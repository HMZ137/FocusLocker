# FocusLocker v1.4.0 发布说明

本次更新涵盖 v1.3.1 → v1.4.0 的全部改动，重点新增**数据迁移、查看验证码、对话命令补全、无 API 本地指令**等能力，并修复一批交互与稳定性问题。

## ✨ 新功能

- **数据一键导出 / 导入**：设置页「数据备份与迁移」可勾选数据集（网站设置、专注设置、Cookie、自定义环境音、文件库、Todos、使用统计、浏览器扩展、用户样式 / 脚本），导出为单个 zip（含 `manifest.json`），导入按勾选还原，环境音连同音频本体一并迁移。
- **查看验证码**：一键临时关闭强制置顶以便查看验证码；入口收纳进设置面板，生产 / 测试模式均可用；每次启动限用 2 次；临时关闭时长由 45s 延长至 75s。
- **对话输入栏命令补全面板**：输入 `/` 弹出暗色玻璃态候选浮层，`Tab` 切下一个、`Shift+Tab` 切上一个、`↑/↓` 同步切换、`Enter` 插入、`Esc` 关闭。
- **无 API 时本地指令自动处理**：未配置 DeepSeek API 时，`/add` `/list` `/timer` `/pomodoro` `/stats` `/quote` `/switch` `/extend` `/rain…/stop` `/screenshot` `/help` 等指令由本机直接执行并回复「已执行」，无需 AI 推理。

## 🛠 修复与优化

- **导出卡死修复**：会话存储损坏导致 `cookies.get` 永久挂起、保存框被锁屏遮挡，已加超时兜底 + 临时降级置顶，导出不再卡死。
- **原生对话框遮挡**：导出 / 导入位置选择及所有文件选择器被锁屏置顶遮挡的问题，统一通过临时降级置顶解决。
- **通知中心高度联动**：高度随铃铛 / 面板位置自适应，不再整屏溢出。
- **会话存储库自愈**：检测到存储损坏时自动备份并清空重建，消除 `quota` / `service_worker` IO 报错反复出现（代价：该分区 cookie / localStorage 清空，需重登网站）。
- **禁用 WebRTC STUN 噪音**：专注场景无需 WebRTC，全局禁用并拦截 stun / turn 请求，消除 `stun.l.google.com` 解析失败日志。
- **扩展加载日志点名**：记录每个扩展名称与 Manifest 版本，MV2 显式告警，便于定位 `ExtensionLoadWarning`。
- **欢迎语按有无 API Key 分两套**：有 Key 强调 AI 对话 / 分析能力，无 Key 提示如何开启 AI。
- **性能与体积**（1.3.2）：启动 IPC 并行化、CDN 库本地化（marked / highlight.js / KaTeX）、移除死依赖。
- **暗色模式重设计**（1.3.2）：整体提亮、拉开背景层次。
- **图标统一 Lucide**（1.3.2）：45 个 SVG 图标统一为 Lucide 开源集，风格一致。
- **通知中心滚动 / 拖拽唤出 / 横幅角落偏好**等多处缺陷修复（1.3.1–1.3.2）。

## 📦 升级指引

覆盖安装即可，用户数据（`userData`）不受影响。建议本机磁盘异常的用户运行 `chkdsk /f` 以根除会话存储损坏。

## ⚠️ 已知问题

- 本机文件系统 / 回收站损坏可能导致会话存储库反复损坏，应用层已加自愈兜底，但建议修复磁盘以治本。
